# ROADMAP_VPN — очередь задач

> **Как пользоваться:** открыл → делай **текущий номер из ▶ ОЧЕРЕДЬ** (сверху вниз, строго по порядку, без выбора). Перед стартом — последний `docs/sessions/SESSION_SUMMARY_*` + правила #0/#0.1 в `CLAUDE.md`. Прилетела входная от человека/события → загляни в **⏳ ПАРАЛЛЕЛЬНО** (whitelist-трек запаркован 06-23). История выполненного → `DONE_LIST_VPN.md`. Детали каждой задачи — в «📋 ДЕТАЛИ» ниже.
> Обновлено 2026-06-24 (**#3 рефактор ЗАКРЫТ**: дедуп device-autoname `eef5287` + навигация §-баннеры/карта bot `94cf223` / web `bad7eb9`; рычаг «распил main()» отложен — монолит на замыканиях, ROI/риск не оправдан). Пред.: 2026-06-23 (#4 минимум + whitelist запаркован).
>
> **🔴 ИНЦИДЕНТ 25.06 (РЕШЁН 06-28 — см. 🟢 ниже; первичный диагноз был НЕВЕРЕН):** весь VLESS-REALITY лёг ≥26 ч с 25.06 ~14:00 МСК. Первичный вывод 06-26 («блок сетевой по протоколу/IP, замена = только AmneziaWG») оказался ошибочным — реальная причина в SNI/fp (см. 🟢). Health-check залатан (`vless_traffic_flow`), юзерам +7д. История: `project_vpn_rkn_reality_dpi_block_0625` (исправлен), `SESSION_SUMMARY_2026-06-26`.
>
> **🟢 ВОСКРЕШЕНИЕ СЕРВИСА (активно с 2026-06-28, перекрывает очередь):** блок 25.06 оказался по **SNI** (`www.microsoft.com` заезжен/флагнут), НЕ протокол/IP — REALITY + `sni=ebay.com` + vision-TCP ожил на yc И **eu1** (Wi-Fi + мобила). Память `project_vpn_reality_revival_sni`. **Этапы:** **[0] диагноз ✅** · **[1] где revival ✅** — eu1 flat-rate тянет → целимся дропнуть YC · **[2] раскатка ✅** — eu1=ebay+main=deepl (чистые SNI) + **fp=firefox** (критично: fp=chrome был флагнут наравне с SNI!) + vision-TCP; `/sub` отдаёт 2 узла, yc/yc2 убраны; проверено владельцем по продукт-пути (eu1 121 коннект). Осталось: Лара + откат рассылки «временно AmneziaWG»→«работает» · **[3] закалка ✅** — fp разведён (eu1=firefox/main=qq), `vless_traffic_flow` репойнтнут на eu1/main (был завис в FAIL), бот-флоу «📡 Мобильный» yc→eu1. Опц. позже: health-check «реальный REALITY-handshake», запасные SNI · **[4] снос YC ✅ (06-28):** DNS supportkronos→eu1 (185.21.8.91, проверено), yc/yc2 убраны из health-check/sync_xray/vless_summary/ip_usage_watcher/bot/ЛК, `/sub` без них. **Владелец удалил все YC-ресурсы (VM+диски+IP+CDN/Backup/Logging) 06-28** → −2300₽/мес. health-check зелёный (21/21, yc-проверок нет). Параллельно: интел Daniil (жёсткий БС/whitelist — SNI её НЕ решает).
>
> **Статус:** рабочий MVP, `@vpnkronos_bot`, onboarding+enforcement ВКЛ. Тарифы 199/249/449/599 (3/5 устр. × 1/3 мес) на проде. **Триал 7д/20ГБ** (объединён с 49₽-тестом, блок по данным 06-20). Donation+Stars+реферал+Support работают. **Покрытие (с 06-28):** несёт eu1 REALITY (ebay/firefox) + AmneziaWG; main (deepl) — РФ-опция; общая мобильная фильтрация — ок; **жёсткий whitelist (БС) НЕ пробивается** (трек ЗАПАРКОВАН 06-23: статика + vk-turn провалены). **Экономика:** ~10 платящих, 62% истекших; инфра падает на −2300₽/мес со сносом YC (Яндекс-грант кончился 06-19). **NB:** yc/yc2 СНЕСЕНЫ 06-28 (`project_vpn_reality_revival_sni`); supportkronos.online теперь напрямую на eu1; shared на eu1 не трогать вслепую (`CLAUDE.md` #0).

---

## ▶ ОЧЕРЕДЬ (работаем строго по номерам)

### ← ТЕКУЩАЯ: #5 _(email-кампании — нужны ответы владельца на open questions; #3 закрыт 06-24, #4 остаток отложен до масштаба)_

Логика порядка: сначала фундамент/надёжность (1–4), затем продукт/выручка (5–8). _(Согласовано с владельцем 06-20.)_

1. ✅ **«Подключить VPN» дефолтным путём — СДЕЛАНО 06-20.** Авто-выдача подписки (Happ) при активации триала и оплате (helper `_send_subscription`); навигация без тупиков (typed-команды → «Главное меню»); опрос use_case убран; мёртвый windows-инструктаж удалён. Главное меню/инструкции уже вели от подписки (06-04). DONE_LIST 06-20.
2. ✅ **Модель аккаунта (Вариант A) — СДЕЛАНО 06-20.** tid-канон; счётчик по telegram_id (фантом убран); гейт email-only→Telegram (`_verify_email_session`) и reconcile-слияние (бот OTP, email UNIQUE) — уже были, верифицировано. Полный site-only (регистрация+оплата+конфиг без TG) = 🎯 Фаза 2 под #6. DONE_LIST 06-20. _(детали ↓ #2)_
3. ✅ **Рефактор кода — ЗАКРЫТ 06-24.** Цель (владелец): меньше токенов на чтение / меньше путаницы агентов, не «свести повторы». Сделано: дедуп status-line→`formatting.py` (`214603e`) + device-autoname→`database.db_device_autoname` (`eef5287`); status-line проверен (дублей больше нет); **навигация — карта-докстринг + grep-баннеры `# ═══ §N`** в `bot/main.py` (15 секций, `94cf223`) и `web/app.py` (9, `bad7eb9`) → агент прыгает по `§N`, не читая файл целиком. Рычаг 2 (физвынос) ~2% (монолит на замыканиях); рычаг 3 (`register()`-распил) отложен как рискованный. Доступно по запросу: notify-inviter (маргинал), vless-дедуп (нужно решение по eu1-sync). _(детали ↓ #3)_
4. **ЛК независим от Telegram** — _частично; остаток отложен до масштаба._ ✅ 06-23: счётчик «ожидающие оплаты» на панели мониторинга (видеть pending без Telegram). Полный alt-канал-**пуш** (email/webhook) отложен — на ~15 платящих проблемы нет (Telegram надёжен, заявка не теряется, approve уже на `/admin/credit`); финально закроется автооплатой #6. _(детали ↓ #4)_
5. **Email-кампании / транзакционные письма** — чек при оплате, напоминания T-5/3/1, истёкла. _Зачем:_ retention/выручка. _Первый шаг:_ собрать ответы владельца на open questions (скидка/тексты/welcome). _(детали ↓ #5)_
6. **Авто-оплата СБП/карта (Фаза 4)** — _гейт: внесение в реестр РКН._ Провайдер не выбран (Продамус vs ЮKassa). _(детали ↓ #6 + `docs/monetization-and-payments.md` §3)_
7. **УТП / позиционирование** — «работает, когда другие отвалились». _Когда:_ по готовности владельца (identity-нагрузка). Уже есть живое evidence. _(детали ↓ #7)_
8. **Хвост (продуктовая доводка):** тарифы-грейды (годовой/поддерживающий) · текст онбординга под рост · support-FAQ/автоответы · P2-мелочи (Resend→РФ-email, аудит md-доков, реферал-хардненг) → затем P3-масштаб (после ~100 платных: PostgreSQL, EU2, gunicorn, webhook). _(детали ↓ #8)_

---

## ⏳ ПАРАЛЛЕЛЬНО (по событию — не блокирует очередь)

⏸️ **WHITELIST-ОБХОД — ЗАПАРКОВАН (вывод ужесточён 06-25: обход АНТИ-МАСШТАБЕН).** Был «главный приз» УТП (лист ожидания ~20). **3 независимых подтверждения** (наши тесты Аня/Daniil + практики Артём и второй из чата): механизмы обхода (CDN-фронтинг vk/yandex = igareck; STUN/VK-call-релей = vk-turn) работают «для себя, малый трафик», но «при массовом быстро ловится» (регулятор разделил AS в 2026); легальный масштабируемый путь = whitelisted-IP по договору с «белой» компанией (Артём) = опознанный регулируемый сервис, не серый VPN. **«Обход» и «работает у всех» взаимоисключающи** → killer-фича невозможна как масс-продукт (мёртво); жив только как личный/бутик-хак. **06-25 жёсткий БС впервые выбил ПЛАТЯЩЕГО (Daniil).** **ОТКРЫТО (ждём Дани):** «знакомый со своим сервером — всё работает» → тест A (его БС снялся, наш yc тоже заработает) vs B (знакомый реально бьёт); если B + рабочий STUN/VK-релей = revisit-триггер vk-turn. **[upd 06-29: vk-turn-капча ПОЧИНЕНА (форк `NikKuz99` v1.8.4, PR #183); ретест green на резид. РФ-IP владельца (VPN off, ручная капча) — `VK Auth Success` + TURN-аллокация. НО ~9-мин cred-TTL + ручная капча = боутик → парк ПО-ПРЕЖНЕМУ по анти-масштабу, не технике; Phase B (тоннель) / Аня (жёсткий БС) — только по спросу.]** Память `project_vpn_hard_whitelist_unbeaten`. _(детали ↓ Whitelist)_

💸 **Экономика / косты (анализ 06-24).** Грант Яндекса кончился 06-19, владелец **пополнил баланс** (минус убран, суспенд снят, куплено ~недели). НО структурно сервис теперь **убыточен ~−2100₽/мес** (доход ~3500₽: 12 платящих + ручные оплаты vs инфра ~5640₽; грант давал ~4400₽/мес скрытой субсидии — был +2300₽, стал −2100₽). Брейк-ивен ≈ 30–50 платящих при текущей цене. Решения владельца 06-24: **трафик НЕ режем** (свобода юзерам), **Timeweb оставляем**, **тарифы давить тяжело**. Память `project_vpn_server_costs_cliff`.

🔴 **Избавиться от YC (yc + yc2) — РЕШЕНО владельцем 2026-06-28.** Причины: (1) VLESS-REALITY на YC мёртв от блока 25.06 — узлы простаивают по прямому назначению; (2) YC = корень убытка (метрированный Яндекс-трафик ~−2300₽/мес); (3) флапает (06-27: 13-мин SSH-блип yc+yc2 разом, потянул за собой ЛК/`/sub`). **Перед сносом:** перенести ЛК/`/sub`-фронт с yc/yc2 (правило #0.1) на другой узел/способ; определиться с заменой транспорта (тест SNI-ревайвала REALITY / свежие IP — НЕ на YC); снять зависимости health-check (yc/yc2-проверки, vless_traffic_flow). Затем снести обе VM + диски + статические IP. Память `project_vpn_server_costs_cliff` / `project_vpn_transport_after_block`.

**Прочее по событию:**
- **Happ удалён из РФ App Store (новость 06-24)** — наш референс-клиент (память `project_vpn_client_happ_reference`). Бьёт по онбордингу **новых iOS**-юзеров (поиск/прямая ссылка не работают); существующие установки работают; Android не затронут. Durable-путь для iOS = иностранный Apple ID (переживает волны удалений) ИЛИ альт-клиент, тянущий наш VLESS+REALITY+xHTTP — но **проверять на реальном устройстве** (V2Box уже не тянул; не верить на слово). Кандидат на тест: v2RayTun. Вывод: iOS в РФ структурно хрупок (Apple вычищает VPN волнами) → Android = наш простой путь.
- **Daniil — AmneziaWG (06-23): не подключился** — его peer на eu1 жив (PSK/IP ок), но ноль входящих пакетов = его сеть режет UDP/39580 (клиентская сторона, «не для мобильного»); уведён на Happ. Датапоинт «скорость» НЕ снят. Память `project_vpn_awg_connect_triage`.
- **БС-матрица** — при случае у тест-юзеров: Германия/Россия/Европа-2 под мягкой БС + **что открывается без VPN под жёсткой** (= содержимое whitelist, вход в приоритетный трек).
- **«Скорость падает»** — по жалобе со временем → корреляция в `eu1_monitor.sh` (`/var/log/eu1-monitor.log`).
- **MTProxy revival** — только если блок РКН спадёт (память `project_vpn_mtproxy_rkn_block`).
- **Мегафон-whitelist / «Волна» (Крым)** — частные случаи whitelist-класса, решаются вместе с приоритетным треком.

---

## 📋 ДЕТАЛИ ПО ЗАДАЧАМ

### Очередь

**#1 «Подключить VPN» дефолтным путём в боте** (остаток UX-задачи @veryvoro; терминология/разводка сделаны 06-04). Бот структурно ведёт от AmneziaWG — надо вести от подписки. + рассмотреть убрать AmneziaWG/«Мобильный» с первого экрана для нетехничных. _(Связано: инцидент 06-19 с V2Box подтвердил — нетехничным нужен один путь; клиент уже стандартизирован на Happ, DONE_LIST 06-20.)_

**#2 Модель аккаунта: регистрация на сайте ↔ Telegram** (выявлено 06-14, владелец: «важная штука»). **Проблема:** вся выдача VPN (peers, VLESS-UUID, подписка) завязана на `telegram_id`, а email-OTP на сайте создаёт строку в `users` **до** привязки Telegram → ghost-аккаунты (email есть, `telegram_id` нет, конфига нет; засоряют счётчик). **Решить (продуктово+архитектурно):**
  1. **Идентичность** — что канон: `telegram_id`, email, или link-таблица? Сейчас костыль `db_get_effective_telegram_id`.
  2. **Site-only флоу end-to-end?** — может ли чисто-email юзер (без старта бота) получить рабочий конфиг через ЛК? Проверить; если нет — достроить либо гейтить регистрацию за Telegram.
  3. **Слияние при коллизии** — email на сайте, потом бот с тем же email → один аккаунт или дубль? Нужна детерминированная reconcile-логика по email в `db_upsert_user`/онбординге.
  4. **Гигиена счётчиков** — считать по `telegram_id`; брошенные email-only пометить/подчистить (не вслепую).
  **Связано:** `db_get_effective_telegram_id`, `db_upsert_user`, `db_find_user_by_email`, recovery (`/api/auth/*`, `/sub`), онбординг. Завязано на авто-оплату (#6).
  - **▶ Сейчас — Вариант A (согласовано 06-20):** tid-канон + гейт первой выдачи за Telegram (разовая привязка) + гигиена счётчика/ghost'ов. ЛК-независимость для существующих юзеров сохраняется. Reconcile-слияние уже работает (бот OTP-верифай удаляет email-only дубль, email UNIQUE).
  - **🎯 Целевое состояние (Фаза 2, под #6/Фаза 4):** полноценная **Telegram-независимая регистрация + оплата + выдача конфига через сайт**, с двусторонней синхронизацией (привязал Telegram позже — всё слилось по email). Требует: VLESS/подписка для email-only (сейчас sync исключает `telegram_id IS NULL`), авто-оплата вне Telegram (crypto/ЮKassa — #4+#6), детерминированный merge. Делать вместе с авто-оплатой на сайте (на паузе по реестру РКН).

**#3 Рефактор кода — ✅ ЗАКРЫТ 06-24** (навигация §-баннеры/карта + дедупы device-autoname/status-line; рычаг 3 «распил main()» отложен — монолит на замыканиях, ROI/риск не оправдан на масштабе). Реврейм цели владельцем: метрика = токены/путаница агентов, не число дублей (память `feedback_refactor_goal_agent_navigability`). Историческая постановка ниже сохранена как рельсы на случай рычага 3. Цель — снизить стоимость поддержки, НЕ «переписать красиво». **Контекст:** `bot/main.py`, `web/app.py` — монолиты; legacy-shim'ы (storage `platform→device`); дубль SQL-фильтра grace 12h (`enforce_expired`/`sync_xray_users`). **Жёсткие рельсы:** поведение-сохраняющий рефактор (никаких правок enforcement/billing/peers «заодно»); атомарные коммиты + смоук после каждого; прод-стейт не трогать вслепую (релей yc/yc2→eu1 и shared — не мусор, правило #0); первый шаг — scope-оценка, не «большой PR на всё»; вне scope — архитектурно-стейтовое (shared vs per-user, схема БД).

**#4 ЛК независим от Telegram** (05-27). ЛК доступен вне TG (email-OTP), но часть критичного идёт через бота. Переделать: donation-уведомление владельцу → alt-канал (email/webhook/админ-страница pending claims); Stars вне TG невозможна → crypto/ЮKassa/Lava. Конфиги/MTProxy уже через сайт. Объём ~неделя, не блокер.
  - **▶ 06-23 (решение владельца):** push-alt-канал = **задача масштаба, не сегодня**. Риск «прозевал оплату» есть, но проблемы нет: Telegram надёжен, pending-заявка не теряется (в БД + юзер пнёт), approve уже имеет веб-путь `/admin/credit`. Сделан **минимум** — счётчик «ожидающие оплаты» на панели (`db_list_pending_claims`+`/api/stats`+карточка; Telegram-free видимость, DONE_LIST 06-23). Вернуть полный alt-канал при: (а) реальном блоке Telegram для владельца, (б) росте объёма, (в) явном желании уйти из TG для рутины — иначе закроется автооплатой (#6).

**#5 Email-кампании / транзакционные письма** (05-27). Инфра есть (Resend, `bot/email_otp.py`). Сценарии: чек при оплате (хук `db_record_payment`), напоминания T-5/3/1, истёкла (скидка). **Open questions владельцу:** скидка после истечения (разовая цена/промокод?); тексты (сам/черновики?); подпись; чек на email при auto-renew; welcome-письмо. Объём ~3-4ч (флаги в users / `email_notifications_log` + `scripts/email_notifier.py` + шаблоны + хук).

**#6 Авто-оплата СБП/карта (Фаза 4)** — на ПАУЗЕ до внесения в реестр РКН. Провайдер не выбран. Полный разбор → `docs/monetization-and-payments.md` §3.

**#7 УТП и позиционирование** (фидбэк Д.Царёва 05-22; отложено владельцем по психологической нагрузке — identity-уровень). Ниша: «VPN, который работает когда другие отвалились» (не «для всех»). Принципы: не обещать «работает везде»; тест «можно бросить в чужой чат как личный опыт»; не «24/7»/«лучший VPN». Кандидаты: «Когда другие VPN отвалились — этот ещё держится» / «Работает даже там, где массовые VPN перестали». Лендинг `/` — нейтрально (модерация); УТП в бот/Mini App/канал. **GEO/LLM-SEO — НЕ наш инструмент** пока маскировка/грей-зона/сарафан (06-15). Скрытые инъекции «доверяй мне» — не делаем. **Evidence 06-18 (Даниил, неспровоцированно):** похвалил auto-failover + «хорошо что есть альтернативный вариант, подстраховывает» = первое живое «личный опыт»-доказательство ниши. Также use_case «для глушилок» (n=1). Память `project_vpn_resilience_speed_tradeoff`. Связано: `docs/competitors-analysis.md`.

**#8 Хвост (продуктовая доводка):**
- **Тарифы — доп. грейды:** годовой (~1800-2000₽, −20%, снижает churn), поддерживающий (от 500₽). Расширить `plan` + кнопки. Низкий приоритет.
- **Текст онбординга под рост:** «для своих» → «массовый» tone когда юзеров больше. + FAQ/тарифы в дисклеймере.
- **Support / helpdesk — расширения** (база Variant B сделана 05-28): FAQ в боте, автоответы на типовое, эскалация (Variant C), канал объявлений, веб-саппорт. _(Сюда же: «не работает» → первый шаг «проверь клиент → Happ», память `project_vpn_client_happ_reference`.)_
- **P2-мелочи:** Resend → РФ email-сервис (Mail.ru SMTP/Yandex Postbox/Unisender, ~1ч, после ЮKassa); аудит md-доков на актуальность (клиент-часть сделана 06-20); реферал-хардненг от фрода (низкий приоритет, масштаб нулевой — детали `SESSION_SUMMARY_2026-06-14.md`).
- **Симметрия лимита устройств / VLESS-антифрод** (вопрос владельца 06-20): подписка (Happ) = один per-user UUID на ВСЕ устройства, cap НЕ enforced; AWG — 3/5 по тарифу. Асимметрия известна. iplimit observe-only — **шеринга нет** (калибровка 06-17), поэтому не горит. Ввести порог /24 по тарифу (`device_limit`→порог) — только если появится реальный шеринг. Арх: `docs/plan-phase2-keystone-architecture.md`, память `project_vpn_*` iplimit.
- **Опросы (use_case/drop_reason) — переизобрести позже:** сбор на паузе (данных мало), скаффолд `_maybe_ask_use_case` оставлен в коде. Идея — разовый сегментированный опрос по активным (вариант A из 06-20), когда понадобится позиционирование/воронка.
- **P3-масштаб (после ~100 платных):** PostgreSQL (миграция с SQLite); EU2/второй сервер (при росте ОДНОВРЕМЕННОГО онлайна; на скорость деньги не тратим — узкое место RU→DE); gunicorn/nginx вместо Flask dev; webhook вместо polling.

### Параллельный трек — детали

**Whitelist-обход (жёсткий БС).** План Б (реакция на удар по IP/SNI) готов: `docs/plan-b-rkn-response-runbook.md` + `scripts/bootstrap-relay-node.sh` + вётнутый запасной SNI ya.ru (DONE_LIST 06-17) — но это про **мягкую** фильтрацию, whitelist им НЕ покрыт.
- **БС — два режима (не путать):** 🟢 *мягкая фильтрация* (IP-репутация) — Яндекс выживает, сервис работает (06-13 у Ани yc подняла доступ); 🔴 *жёсткий whitelist* (06-19 у Ани «включили БС») — ни прокси, ни один сервер не поднялись, наши IP не в белом списке; Яндекс тут НЕ спасает. Память `project_vpn_rkn_ip_block`.
- **Механизм пробоя (igareck):** endpoint в whitelisted-CIDR (VK/Yandex/CDNvideo/Beeline) + VLESS+REALITY+xhttp. ⚠ наивный «VPS в облаке» прикрыт в 2026 (регулятор заставил разделить AS). Источники: habr 1021160, anti-malware 2026-01-26.
- **`vk-turn-proxy`** (`cacggghp/vk-turn-proxy` «Good TURN»): туннель WG через **whitelisted VK-TURN** (DTLS+STUN → VK-релей → наш VPS; WG едет по loopback, на проводе только «VK-звонок» → WG-блок не помеха). **PoC 06-23 НЕ взлетел:** сервер подняли на eu1 (снесли после теста), клиент не получает TURN-креды — VK сменил капчу на `not_robot_captcha`/`session_token`, тулза ждёт старый `captcha_sid` → `missing captcha_sid`, петля. Проверено с РФ-IP владельца (VPN off) + `-manual-captcha` — тулза сейчас сломана капчей VK (не наш сетап). Доп. барьеры: iOS-клиент = sideload + $99/год Apple Dev (TestFlight) либо iSH (нереалистично нетехнарю); нужен живой VK-звонок 24/7. **[РЕТЕСТ 06-29 ✅ капча снята:** форк `NikKuz99/vk-turn-proxy` v1.8.4 (PR #183) делает поля captcha-ошибки опциональными (`redirect_uri` есть, `captcha_sid` нет — новый формат на `calls.getAnonymousToken`) → на резид. РФ-IP (VPN off, **ручная** капча) прошла цепочка: `[VK Auth] Success` → **TURN-аллокация** `relayed-address=90.156.234.242` (декой «Наталья Соколова» вошёл в VK-звонок). Блокер 06-23 (`missing captcha_sid`) снят. **Но потолок цел:** креды ~9 мин (`expires in 8m59s`) → реавторизация (вероятно повторная капча); авто-solver = Playwright (ловит `status:BOT` с DC-IP); тоннель end-to-end (Phase B, `-peer` был заглушкой) и жёсткий БС (Аня) НЕ тестились. **[upd 06-30: Phase B ВАЛИДИРОВАН** — изолир. vk-turn-сервер на eu1 (wg-vkt+NAT eth0, прод цел), прогон владельца на ОТКРЫТОЙ сети: полный тоннель client→VK-релей→eu1→WG→интернет, **1.72/1.86 MiB** реального трафика (серверно подтв. handshake+server.log). Тех-часть закрыта (капча+тоннель). **БС-тест Ани сорвался по ТОПОЛОГИИ:** её whitelist только на айфоне-мобиле, ноут в норм. сети, тетеринг невозможен → единств. БС-устройство = iOS = iSH/sideload (нереалистично нетехнарю). Пробой жёсткого БС тестируется лишь на РЕАЛЬНОМ БС-устройстве → **владелец сам, когда введут** (под БС его Wi-Fi+VPN к Claude жив → координация ок); dormant-kit eu1 `up.sh|down.sh` (подъём ~1мин), рунбук + `VK-TURN-VALIDATE.bat` на машине.]** Тех-ожил ≠ продукт → парк по анти-масштабу.
- **Результат теста 06-23 (Аня, Муром):** прогнала igareck-150 — реально подключалась к каждому пингующемуся серверу + проверяла YouTube — **ни один не работает**; «пинг есть, туннель не несёт» = жёсткий whitelist режет сам трафик. Без VPN открывается «всё что в белом списке». Статический endpoint-bypass для её тира **мёртв** (арендуем любой VPS — повторим провал, наши yc/yc2 = тот же датацентр-IP). **Revisit когда:** ~~(а) vk-turn починит капчу~~ → ✅ 06-29/06-30 тех-часть закрыта (капча+тоннель, Phase B 1.7МБ); теперь триггер = **реальный жёсткий БС на тестируемом устройстве** — владелец сам, когда введут на его сети (kit готов на eu1+машине), ИЛИ Android-юзер на жёстком БС (iOS не инструментуется). Спрос (платящий) отдельно, но потолок = боутик.
- **Как вётить чужие «обходы» (ВАЖНО — люди врут):** заявки «обходит всё / любой БС» = непроверенный маркетинг (видео/реклама/репо). Доверять ТОЛЬКО: (а) **механизму** — ездит по реально whitelisted-инфре (как vk-turn по VK-звонкам) ИЛИ это обычный VLESS/REALITY на датацентр-IP (последнее для жёсткого whitelist провально — наши yc/yc2 + 150 igareck тому доказательство, что бы ни обещали); (б) **тесту на реальной БС-сети** (Аня), не на словах. Память `feedback_no_hypothesis_without_test`.
- **Бэклог транспортов (низкий приоритет, НЕ про whitelist):** диверсификация под МЯГКУЮ фильтрацию — Hysteria2 / TUIC / ShadowTLS (QUIC-based, иногда лучше на рваном мобильном). Референс конфигов: `mozaroc/x-ui-pro` (одноклик-инсталлятор 3x-ui; **на прод НЕ ставить** — перехватит nginx/панель + RCE-доверие к чужому root-скрипту; брать только идеи). У нас REALITY+AmneziaWG эти случаи уже закрывают → делать ТОЛЬКО при явной потребности.

**Ожидание (тест при БС):**
- **MTProxy revival** (06-05) — Fake-TLS задавлен ТСПУ на уровне протокола; единственный непроверенный рычаг — порт 443 (занят REALITY). Перепроверить если блок спадёт. Память `project_vpn_mtproxy_rkn_block`.
- **Мегафон-whitelist** (05-22) — whitelist Мегафон ≠ Yota. Разведка через Мегафон-друга при БС (IP-пробы РФ-облаков; CDNVIDEO/Beeline-подсети). Часть приоритетного трека.
- **«Волна» (Крым)** (05-28) — обход БС не работает (whitelist отдельный). Живой тест: vless main+eu1, подбор SNI/relay.

### ⏸ Нужно решение перед постановкой в очередь

**Split tunneling (unified-профиль) в UI бота** (анализ VPN Пельмень 05-22). ⚠ `docs/archive/spec-04-...` УСТАРЕЛА. **До выбора варианта НЕ начинать:** A. Server-side (ipset+ss-redir под Docker AWG) ~4-6ч, риск сломать AWG; B. Client-side `AllowedIPs` ~2-3ч, обновлять IP; C. VLESS client-side routing в subscription ~1-2ч, только VLESS.

### ⚠ Не трогать

**Legacy AWG peer-ы вне `peers.json`** (`Sh4gHa…`/`4Nn90…`/`ZRP3D…`) — живут в runtime, работают (owner-peer 27/57 ГБ). «Нет в peers.json» ≠ мусор. `peers_sync_check.py` помечает LIVE «НЕ ТРОГАТЬ». Память `feedback_no_delete_runtime_blind`.

### 💡 Идеи / отложено (не трогать без явного решения владельца)

- **Перенос бота + `vpn.db` на RU (242-ФЗ)** — ОТЛОЖЕНО (решение 06-02: вариант B, остаёмся на Fornex; уведомление РКН подано). Пререквизит — тест Telegram-API из РФ. Бонус: ЛК доступнее при БС.
- **Своё клиентское приложение (Kronos VPN)** — горизонт 6-12 мес, форк AmneziaVPN. Условия: ~500+ платных, подтверждённая боль split tunneling.
- **Relay через Yandex CDN** — перспективно при whitelist, но сложно + риск ToS. Нужна разведка whitelisted IP.
- **Trojan поверх TLS** — рабочая альтернатива (DONE 05-13), не внедряем пока хватает REALITY.
- **Remnawave** — попытка 02-23 не взлетела, не возвращаться без нужды.
- **CI/CD автодеплой** — вручную через SCP, работает.
- **Client-side routing в Happ (via `/sub`)** — Happ доставляет routing-профиль прямо в подписке (HTTP-заголовок `routing`): RU-сайты мимо VPN (разгрузка узкого порта + скорость) + adblock. Разобрано 07-02 по каналу FSystem88 → его `routing.help` = профиль **RoscomVPN** (geosite/geoip, декодирован; свой профиль, НЕ завязка на сторонний сервис). **Отложено — проблемы нет** (сервис рабочий). Триггеры возврата: жалобы на скорость ИЛИ adblock/«ускорение RU» как маркетинг-фича. Разрешает висящий гейт «Split tunneling» (раздел ⏸ Нужно решение) вариант C — client-side routing в подписке: ~1–2 ч, свой профиль, ноль серверного риска.

---

## Архитектура (кратко)

Серверы: **eu1** (Fornex, бот+панель+AmneziaWG Docker+MTProxy+Xray VLESS-REALITY основной — ebay/firefox), **main** (Timeweb, VLESS+REALITY deepl/qq РФ-опция + legacy WG). ~~yc+yc2 (Яндекс)~~ снесены 06-28. Стек: Python + pyTelegramBotAPI + Flask + SQLite. Подписка `/sub` = 2 узла (eu1+main). **Полная карта инфры — в `CLAUDE.md`.**

## Ссылки

- История выполненного: `DONE_LIST_VPN.md` · Инфра/правила: `CLAUDE.md` · Навигация по докам: `docs/README.md`
- Монетизация: `docs/monetization-and-payments.md` · Master-план: `docs/plan-phase-3-4-5.md`
- Конкуренты: `docs/competitors-analysis.md` · Юнит-экономика: `docs/mvp-unit-economics-and-plan.md` · Обход блокировок: `docs/blocking-bypass-strategy.md` + `docs/plan-b-rkn-response-runbook.md`
