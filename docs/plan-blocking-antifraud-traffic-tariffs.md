# Мастер-план: блокировки РКН · анти-фрод · трафик · тарифы

> Создан 2026-06-09. Это **handoff-документ** для агентов, заходящих в следующую
> большую задачу. Самодостаточен: контекст + текущее состояние + план по 4 темам.
> Контекст сессий: `docs/sessions/SESSION_SUMMARY_2026-06-0{5,6}.md`. Память:
> `project_vpn_rkn_ip_block`, `project_vpn_mtproxy_rkn_block`, `feedback_no_hypothesis_without_test`.

## Краткая картина инфраструктуры (на 2026-06-09)

- **Серверы (REALITY):** eu1 (Fornex/Германия `185.21.8.91`), yc (Яндекс-Облако `158.160.236.147`), **yc2 (Яндекс `84.252.136.139`, новый резерв)**, main (Timeweb `81.200.146.32`).
- **AmneziaWG** на eu1 (UDP 39580) — отдельный протокол, держится где UDP проходит.
- **Подписка** `/sub/<token>` (nginx `supportkronos.online:8443` → Flask) отдаёт base64-список 4 vless-серверов; HAPP/Streisand сами выбирают рабочий (по пингу) + авто-failover.
- **Per-user UUID** на main/yc/yc2/eu1 (телеметрия `vless_user_traffic`). Енфорсмент: `enforce_expired.py` (AWG soft-revoke) + `sync_xray_users.py` (VLESS revoke по grace 12h).
- **~56 юзеров**, в основном триалы (оплат пока нет — норма для ранней стадии).

---

## 1. Блокировки РКН — резильентность

**Диагноз (durable, см. `project_vpn_rkn_ip_block`):** РКН режет **по IP-репутации, НЕ по транспорту.** На фильтрующих сетях (T2-тип) иностранные IP (Fornex) и VPN-хостинги (Timeweb) блокируются; **крупное РФ-облако (Яндекс) проходит.** Транспорт tcp↔xhttp не влияет (доказано: eu1 и yc идентичны кроме IP — eu1 не работает, yc работает). Мегафон-БС: не пускает ни один облачный IP, только домены — тяжёлый гэп.

**Сделано:** yc2 (2-й Яндекс REALITY, клон yc) — резерв + разгрузка yc для filtered-сетей.

**План:**
- [ ] **Перенести subscription-fetch URL** с `supportkronos.online:8443` (немецкий IP) на РФ-IP (Яндекс) — сейчас под БС подписка не обновляется (HAPP берёт кэш). Reverse-proxy `/sub` на yc/yc2.
- [ ] **Ещё Яндекс-эндпоинты** по мере роста нагрузки/блокировок IP (Яндекс — выживший; другие РФ-облака VK/Mail/Selectel под Мегафон-БС НЕ прошли).
- [ ] **Сигнал блокировки = жалобы юзеров по оператору** (не health-check — он видит только «сервер поднят»). Завести лёгкий канал сбора (support).
- **НЕ делать:** гнаться за транспортом (gRPC и т.п.) для решения IP-блока — не поможет. Не менять проверенные каналы по непроверенной теории (урок 06-06: дважды зря гонялись за транспортом).
- **Мегафон-БС** — отложено (нужен whitelist-friendly эндпоинт, пока не найден).

---

## 2. Анти-фрод (шеринг ссылок)

**Контекст:** юзеры шерят ссылки (подтверждено фидбэком). Нужно (а) видеть шеринг, (б) ограничивать по тарифу.

**Инструмент (готов):** `scripts/access_audit.py` — энумератор всех кредов на всех серверах + инвариант «кред ↔ активный юзер И отзываем». Текущий прогон: единственная untracked-зона = **eu1 shared-пул (11 UUID)**; main/yc/yc2 — 100% per-user.

**Фрод-зона eu1 (закрытие):** ✅ **ЗАКРЫТА 2026-06-10.** Этапы 0-2 (per-user backfill + grace-sync) + **Этап 4** (`sync_eu1_vless.py --no-shared` → удалены 11 shared + истёкший 7882817110, все 3 inbound = 50 per-user) + durable cron `17 */6` + `access_audit` расширен на yc2. Верификация: 0 проблемных кредов на всех 4 серверах. Перед удалением — pre-фикс: переупорядочили подписку (Яндекс первыми, см. §1) + тест владельца. **Этап 3 broadcast** «переподключись» — owner отправляет вслед (straggler'ы на старых shared, особенно CDN). `/migrate_reset` (admin) — не прогонялся, не нужен.

**Лимит устройств (ключевая анти-фрод задача, НЕ начата):**
- [ ] **Per-device слоты** вместо per-platform (сейчас ключ = `{tid}:{server}:{platform}`, iPad+iPhone = один `ios`). Нужны именованные устройства.
- [ ] **Xray iplimit** — ограничение числа одновременных IP на UUID. У **3x-ui это встроено** («Лимит IP» + «Лог IP-адресов») — взять оттуда механизм. Это и есть enforcement лимита устройств.
- Это **keystone-задача**: разблокирует и тарифы (семейный/соло), и анти-фрод.

---

## 3. Трафик-мониторинг

**Есть:** панель показывает per-user VLESS-трафик (`vless_user_traffic`, lifetime) + AWG (`traffic_accounting`). `access_audit` — аудит кредов. `health_check` (cron */15) — инфра. `eu1_monitor` — нагрузка eu1.

**План:**
- [ ] **Аномалии-шеринг:** один UUID + много distinct IP = шеринг (после iplimit — лог IP даёт это явно).
- [ ] **Fair-use пороги** на трафик (для тарифов / защиты канала).
- [ ] Per-server разгрузка видеть (yc мелкий/OOM-prone — следить за RAM yc/yc2).

---

## 4. Тарифы

**Решено (с Аней):** 2 тарифа — **199 ₽/мес базовый**, **149 ₽/мес при оплате за 3 мес** (447₽/3мес). Год/«навсегда» — позже (рано, ноунейм). Реферал +14 дней (есть).

**Лимиты устройств (рыночная норма):** соло **3 устр.**, семейный **5 устр.** (семейный — выгоднее за устройство, чтобы покупали). **Блокер: per-device + iplimit (см. п.2).**

**Бенчмарк:** `docs/competitors-analysis.md` (рынок 150–330 ₽/мес, 3–5 устройств; конкурент 150₽/устройство с авто-failover + БС-friendly).

**Порядок:** сначала **per-device + iplimit (п.2)**, потом включать тарифы (иначе лимит на честном слове).

---

## Зависимости и порядок работы

```
per-device слоты + Xray iplimit (п.2)  ← KEYSTONE
        ├─→ Лимит устройств → Тарифы (п.4: 199 / 149)
        └─→ Анти-фрод enforcement (резать шеринг)

РКН-резильентность (п.1) — идёт параллельно (yc2 done; далее subscription-fetch на РФ-IP)
Фрод-зона eu1 Этапы 3-4 — ждёт спада РКН-волны
Трафик-аномалии (п.3) — после iplimit (лог IP даёт сигнал)
```

### ▶ Первым делом (в самом начале следующего захода)

1. ~~**Досинхронизировать yc2**~~ — ✅ **DONE 2026-06-10.** В `sync_xray_users.py` добавлена запись `yc2` в `SERVERS` (ssh yc2 / inbound `vless-xhttp` / db_column `vless_uuid_yc` / sudo) + `--all` → `[main, yc, yc2]` (cron `--all --no-shared` теперь покрывает yc2). В `health_check.py` — yc2 в `REMOTE_HOSTS`/`REMOTE_CHECK_PLAN` (reachable/xray/:443/disk) + `vless_config_consistency` сверяет yc2 с `db_yc`. Проверено на проде: dry-run + реальный sync yc2 (50 clients, validate OK, restart OK) + health_check dry-run = 27/27 green (`cfg_yc2=50, diff 0`). Дрейф клона закрыт.
2. ~~**Фрод-зона eu1 Этапы 3-4**~~ — ✅ **DONE 2026-06-10.** Pre-фикс подписки (Яндекс первыми) + тест владельца → Этап 4 (`--no-shared`, удалены 11 shared + 7882817110) + durable cron. 0 проблемных кредов на всех 4 серверах. Этап 3 broadcast — за владельцем (вслед).

**Затем — keystone большой задачи:** **per-device + iplimit** — разблокирует тарифы (п.4) И анти-фрод (п.2). Изучить механизм 3x-ui (`iplimit` + лог IP), спроектировать per-device слоты (миграция ключа peer'а `{tid}:{server}:{platform}` → именованные устройства), затем тарифы 199/149.

**UX-долги:** MiniApp Amnezia-конфиг ✅ пофикшен 06-09.
