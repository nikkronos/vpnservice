# SESSION SUMMARY — 2026-06-06

> Марафон. Этап 2 фрод-зоны (eu1 → per-user + в подписку). Затем РКН-волна:
> диагностика показала **IP-репутационную блокировку, не транспорт** — транспортный
> детур (eu1+main на xHTTP) откачен. Enforcement-тест прошёл + 3 UX-фикса.
> **Главный вывод дня: реальный План Б = РФ-облачные эндпоинты.**

---

## TL;DR

- ✅ **Этап 2 фрод-зоны:** eu1 переведён на per-user (backfill 50/50, `sync_eu1_vless.py`, в подписку как 🇩🇪). Этапы 3-4 (broadcast + удаление shared) **на ПАУЗЕ** до спада волны.
- 🔑 **Главный диагноз:** РКН душит **по IP-репутации, НЕ по транспорту.** yc (Яндекс-Облако, РФ) работает; eu1 (Fornex, Германия) и main (Timeweb, РФ) — блокируются на сети владельца (T2). Транспорт (tcp↔xhttp) не влияет: мигрировали eu1+main на xHTTP — не помогло — **откатили на tcp**.
- ✅ **Мегафон-БС разведка** (друг, активные БС): под БС не пускает **ни один** облачный IP (Timeweb / Mail.ru Cloud / VK Cloud / Selectel / наш main) — только домены (`mail.ru`). Известный тяжёлый гэп, не регресс.
- ✅ **Enforcement-тест** (@tosha8686): отзыв сработал чисто (3 peer, 0 ошибок, уведомление, auto-restore готов). Вскрыл 3 UX-бага — все пофикшены (`7ec1b39`).

## Этап 2 фрод-зоны — сделано (eu1 per-user)
- Backfill `vless_uuid_eu1` (50/50, `vless_uuid_backfill.py` расширен). `sync_eu1_vless.py` — локальный, 3 inbound, grace сохраняет старых.
- eu1 в подписку: `_build_subscription_links` + env `VLESS_EU1_SHARE_URL` → метка 🇩🇪 Германия.
- `access_audit.py` (Этап 0): единственная untracked-зона = eu1 shared (11); main/yc 100% per-user, AWG чисто.
- **Этапы 3-4 (broadcast → `sync_eu1_vless.py --no-shared`) — ПАУЗА** до спада РКН-волны (не рубим fallback'и в шторм).

## РКН-волна 06-06 — диагностика (ключевое, durable)
- **Транспортный детур (откачен):** eu1+main мигрированы tcp→xHTTP по теории «режут TCP-REALITY». Ре-тест опроверг: eu1-xhttp всё равно не работает, а yc-xhttp (тот же SNI) работает. → **различие = IP, не транспорт.** Откат: конфиги из бэкапов (`*.bak.xhttpmig.*`), env, sync-скрипты вернулись на tcp+vision. yc не трогали.
- **Паттерн IP-блокировки:** РФ-крупное-облако (Яндекс) проходит; иностранное (Fornex/Германия) + VPN-хостинг (Timeweb) — режется. На сети владельца (T2) из 3 серверов подписки работает только yc.
- **Урок:** перед миграциями не менять проверенные каналы по непроверенной теории. Дважды погнался за транспортом — ошибка, откатил.

## Enforcement-тест + 3 фикса (`7ec1b39`)
1. `enforce_expired.py`: уведомление отзыва теперь с inline-кнопкой «💳 Продлить подписку» (`pay_show`).
2. ЛК: `subBlock` («Подключить VPN») гейтится по активности — неактивному вместо мёртвой ссылки «продли подписку → появится ссылка». Кэш-бамп `recovery.js?v=20260606`.
3. Панель: статус «истёк» при `days_left<0` (было «активен · −1д»).

## Состояние 3 задач владельца (честно)
1. **Отрубить ссылки не-перешедших в новый бот:** ЧАСТИЧНО. main/yc shared убраны (Этап 7, 03.06); **eu1 shared — на паузе** (грейс). `/migrate_reset` существует, не прогонялся на текущих не-migrated.
2. **Уникальные per-user ссылки + мониторинг:** В ОСНОВНОМ ГОТОВО. main/yc — per-user + телеметрия (Этап 7). eu1 — per-user добавлен сегодня. Это самая закрытая задача.
3. **Лимит устройств по тарифам:** НЕ начато. Нужен per-device (слоты вместо платформ) + Xray iplimit (ROADMAP P2).

## ▶ СЛЕДУЮЩИЙ ЗАХОД — приоритеты пересобраны
1. **🔴 Резильентность (URGENT): РФ-облачные REALITY-эндпоинты** (Яндекс-тип) для T2/МТС/Билайн — сейчас иностранные/Timeweb IP блокируются, работает только yc (один мелкий сервер, без резерва). Это про «могут ли юзеры вообще подключиться».
   - Подзадача: **subscription-URL** сейчас `supportkronos.online:8443` на **немецком IP** → под БС не фетчится (HAPP использует кэш). Перенести фетч-URL на РФ-IP.
2. **Фрод-зона Этапы 3-4** (eu1 broadcast + `--no-shared`) — когда волна спадёт. Закрывает задачу №1.
3. **Device-limit (per-device + iplimit) + тарифы** — задача №3, отдельным блоком.
4. **Megafon-БС** — тяжёлый гэп (ни один облачный IP не проходит), отложен.

Свежие сессии: `SESSION_SUMMARY_2026-06-05.md`.
