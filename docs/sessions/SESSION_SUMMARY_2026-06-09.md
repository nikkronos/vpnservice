# SESSION SUMMARY — 2026-06-09

> Приоритет 1 (РФ-облачный резерв) выполнен: **yc2** (2-й Яндекс REALITY) в подписке,
> протестирован на T2. + прокси возвращён (волна спала), MiniApp-фикс, кнопка в
> напоминаниях, мастер-план для агентов, **переезд VPN-проекта** в `Projects/In progress/VPN`.

---

## TL;DR
- ✅ **yc2 «Европа-2»** (`84.252.136.139`, static IP, swap, клон yc) — в подписке, **протестирован на T2 → работает**. Приоритет 1 (резерв для filtered-сетей) выполнен. `35810b2`.
- ✅ **Прокси возвращён** в бот+ЛК (РКН-волна по прокси спала, наша eu1:8444 ожила) + VPN-фолбэк. `2be72b9`.
- ✅ **MiniApp Amnezia-конфиг** — в TG WebApp QR + копия текстом (скачивание не работало). `8183fba`.
- ✅ **Кнопка «💳 Продлить подписку»** в напоминаниях T-7/3/0. `5cc3e1b`.
- ✅ **Мастер-план** (блокировки/анти-фрод/трафик/тарифы) — handoff-док для агентов. `456a164`.
- ✅ **Переезд:** `Projects/VPN` → `Projects/In progress/VPN`.

## yc2 (Приоритет 1 — РФ-облачный резерв)
Диагноз 06-06 (`project_vpn_rkn_ip_block`): РКН режет по IP-репутации; Яндекс-IP выживает, иностранные/Timeweb — нет. Решение — 2-й Яндекс-эндпоинт.
- VM создана владельцем (Яндекс, **static IP**, 2 ядра, 960 МБ + swap 1G, Ubuntu 24.04). Доступ через Fornex-jump, алиас `ssh yc2`.
- yc2 = **клон yc** (scp config.json): те же REALITY-ключи + 48 per-user UUID (`vless_uuid_yc`).
- В подписку: `config.py vless_yc2_share_url` + `app.py` «🇪🇺 Европа-2» (server_id=`yc`) + env `VLESS_YC2_SHARE_URL` (yc-URL с IP yc2).
- **Тест владельца (T2): «Европа-2» работает.** eu1 «Германия» — TLS-ошибка (IP-блок немецкого, ожидаемо).
- Эффект: резерв (если IP yc забанят — yc2 держит) + разгрузка мелкого OOM-prone yc.

## Прочее
- Прокси: РКН-волна по прокси спала, наша инфра ожила → вернули ссылку+QR в бот/ЛК, оставили VPN-фолбэк-кнопку (волны повторятся).
- MiniApp: TG WebApp не качает файл (открывает) → для iOS/ПК даём QR + «Скопировать конфиг».
- 56 юзеров, 0 оплат — норма (триалы; конверсия/отвал с ~10.06 при истечении).

## Переезд проекта
`Projects/VPN` → `Projects/In progress/VPN` (по `In progress/MOVE_VPN_PASTUHIBOT_INSTRUCTION.md`, файл оставлен для другого агента — он удалит). Обновлены: root `.gitignore`, ссылки в root CLAUDE.md / PROJECTS.md / COMMIT_GUIDE.md / ROAD_MAP_AI.md, VPN CLAUDE.md путь. Чужие незакоммиченные правки (другой агент) не трогал — переезд их сохранил.

## ▶ Первым делом следующий заход (см. `docs/plan-blocking-antifraud-traffic-tariffs.md`)
1. **Досинхронизировать yc2** в `sync_xray_users` + `health_check` (~20 мин — иначе статичный клон дрейфнет при оплатах/истечениях).
2. **Фрод-зона eu1 Этапы 3-4** — когда подтвердится спад РКН-волны.
3. **Keystone:** per-device + iplimit → тарифы (199/149) + анти-фрод.

Свежие сессии: `SESSION_SUMMARY_2026-06-05.md`, `SESSION_SUMMARY_2026-06-06.md`.
