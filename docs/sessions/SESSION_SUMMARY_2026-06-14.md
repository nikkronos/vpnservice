# SESSION SUMMARY — 2026-06-14

> Доработки админ-панели по фидбэку владельца + диагностика вопросов по прод-БД
> (read-only) + находка по модели аккаунта. Предыдущая (06-13): порядок в
> документах + БС-тест — `SESSION_SUMMARY_2026-06-13.md`.

## TL;DR
- ✅ **Админ-панель переразложена и дополнена** (несколько итераций, всё на проде):
  сервисы — узкий блок слева; справа 2 ряда карточек (Активность: 24ч/7д/30д/email/mtproxy/трафик AWG/VLESS; Сводка: пользователи/конфигов/с подпиской/на триале); заголовки блоков убраны; список во всю ширину; контейнер 1080→1320.
- ✅ **Новые метрики:** «с подпиской» (11) / «на триале» (18) — `db_count_subscription_split()` (paid = succeeded-платёж; trial = активен без оплаты). VLESS-колонка → per-user трафик числом. Подсветка 4 owner-аккаунтов.
- ✅ **Счётчик «пользователей» 61→58** — теперь по `telegram_id` (`tg_users`), совпадает с таблицей.
- ✅ **Находка + чистка ghost-аккаунтов:** 3 email-only без `telegram_id` (61 в БД vs 58 в таблице). Деактивированы 2 брошенных (227 без подтв. email, 371 без доступа), `active=0`, бэкап БД, платежей нет. id 69 (грандфазер-плейсхолдер 2099) оставлен.
- ✅ **ROADMAP +2 задачи:** P1 «Модель аккаунта: сайт ↔ Telegram», P2 «Реферал — хардненг от фрода».

## Ответы на вопросы владельца (по коду + прод-БД, read-only)
- **«40 дней» у @yanochkina99 / @n_alitair_b** — не баг: n_alitair пригласил yanochkina (её `referred_by` = его код); она оплатила 200₽ 06-10 → реферал-бонус +14 обоим; оба ещё и сами оплатили 30 дн → 44 дн → истекает 24.07.
- **Прокси-колонка** — отметка нажатия «Прокси Telegram»/`/proxy` **в боте** (`db_update_proxy_requested_at`, `bot/main.py`); в ЛК не пишется.
- **eu1** — это **сервер** (Fornex, Германия). Happ цепляется к VLESS на нём («Германия» = прямой вход) и к yc/yc2 («Европа», выход через eu1 = релей). AmneziaWG = отдельное приложение AmneziaVPN.
- **Реферал-фрод:** гейта «только новый юзер» нет; `db_set_referred_by` блокирует лишь повторную привязку и свой код на своём же аккаунте. Дыры: retroactive-реферал у существующих, кросс-аккаунт само-реферал, нет лимита на код. Смягчает: бонус только после реальной оплаты; пользуются 4 юзера, 2 выплаты → не срочно (P2).

## Модель аккаунта (главная находка, → ROADMAP P1)
Выдача VPN (peers/VLESS-UUID/подписка) завязана на `telegram_id`; email-OTP на сайте создаёт юзера ДО Telegram → ghost-аккаунты (email есть, tid/конфига нет). Site-only регистрация **не даёт рабочего VPN** (проверено: у всех 3 ghost-ов 0 артефактов конфига). Открыто: канон идентичности (tid/email/link), функционален ли site-only end-to-end, слияние по email при коллизии, гигиена счётчиков. Завязано на Фазу 4 (оплата на сайте).

## Файлы (коммиты на main)
`web/app.py` (+`tg_users`, +`db_count_subscription_split` в /api/stats), `bot/database.py` (+`db_count_subscription_split`), `web/templates/index.html`, `web/static/main.js`, `web/static/style.css`. Деплой `vpn-web` по pre-deploy checklist (бэкапы + AST + рестарт + смоук). Прод-БД: 227/371 `active=0` (бэкап `vpn.db.bak.ghostclean.*`).

## ▶ Следующий заход
ROADMAP актуален. Приоритеты: iplimit Stage 2 (~06-17), авто-вердикт eu1-shared (~06-16), сбор drop_reason/use_case (~06-20). Крупные заходы: рефакторинг кода (после 23.06), порядок в документах — завершён 06-13. Новое в P1/P2: модель аккаунта, реферал-хардненг.
