# SESSION SUMMARY — 2026-06-23

> Предыдущий: `SESSION_SUMMARY_2026-06-20.md`. Длинный заход: поддержка → #4-минимум + фидбэк → большое whitelist-расследование (итог: трек запаркован).

## TL;DR
- 🛟 **Daniil AmneziaWG** «не подключается»: его peer на eu1 жив (PSK/IP ок), но **ноль входящих пакетов** = его сеть режет UDP/39580 (клиентская сторона, «не для мобильного») → уведён на Happ. Наша сторона чиста (eu1 обслуживал других). Память `project_vpn_awg_connect_triage`.
- ✅ **Очередь #4 (минимум):** счётчик «ожидающие оплаты» на панели мониторинга (`db_list_pending_claims` + `/api/stats` + карточка) — видеть pending-оплаты без Telegram. Полный alt-канал-**пуш** отложен (решение владельца). + `/admin/credit` теперь закрывает заявку (счётчик согласован) + поправлен устаревший URL панели в CLAUDE.md (`:8443/admin`, не `:5001`). Коммиты `9d89648`, `a0da4d7`.
- 🪞 **Фидбэк-разбор:** агент повёл в #4-имплементацию до проверки ценности. Договорённость: открывать задачу гейтом «боль или гипотеза? что если НЕ делать? лучший ли это рычаг?» + дефолт = минимум/ничего, scope повышает владелец. Тег при постановке: «боль» vs «страховка». Память `feedback_risk_not_problem_ship_minimal`.
- ⏸️ **WHITELIST-ТРЕК ЗАПАРКОВАН** (главный итог). Рабочего обхода жёсткого БС сегодня **нет**. Память `project_vpn_hard_whitelist_unbeaten`.

## Daniil — AmneziaWG triage
Скрин: PC-клиент AmneziaVPN висит «Подключение…» к eu1. Диагностика через `awg show awg0`: сервер жив (4 handshake/5мин). Сначала смотрел не того (`Danila13if`); реальный Daniil = `486384359` (mautlew@gmail.com), владелец выдал +90д. Его eu1-peer создан, active, в runtime — но **без endpoint/handshake/transfer** = пакеты не доходят = его сеть/firewall. → Happ. Триаж-шаблон в память.

## Очередь #4 + фидбэк
Обсуждали alt-канал для donation-уведомления. Владелец: «риск есть, проблемы нет — задача ради задачи» (Telegram надёжен, заявка не теряется в БД, approve уже есть на `/admin/credit`, ~15 платящих). Согласились на **минимум**: счётчик pending на панели. Затем фоллоу-ап: `/admin/credit` помечает заявку approved (раньше оставлял «призрак» в счётчике). Деплой vpn-web проверен (active, smoke ок).

## Whitelist-расследование (главное)
Триггер: Аня (Муром, near-constant жёсткий whitelist) + лист ожидания ~20 («обход разблокирует доступ любому» — спрос реальный).
- **Статика (igareck «БЕЛЫЕ СПИСКИ Mobile-150»):** Аня реально подключалась к каждому пингующемуся серверу + проверяла YouTube → **ни один не работает**. «Пинг есть, туннель не несёт» = whitelist режет сам трафик. 2 семейства (CIDR+порт80/WS; REALITY под `x5.ru`/`vk.com`) — оба провалены. Статический endpoint-bypass для её тира **мёртв**.
- **vk-turn-proxy PoC** (`cacggghp/vk-turn-proxy` «Good TURN», 3838★): туннель WG через whitelisted VK-TURN (WG по loopback, на проводе «VK-звонок» → WG-блок концептуально не помеха). Сервер подняли на eu1 изолированно, **снесли после теста** (прод не тронут). Блок: клиент не получает TURN-креды — **VK сменил капчу** (`not_robot_captcha`/`session_token`), тулза ждёт `captcha_sid` → `missing captcha_sid`, петля. Проверено с **РФ-IP владельца (VPN off) + `-manual-captcha`** → тулза сломана новой капчей VK (не наш сетап/IP/VPN; не датацентр-артефакт).
- **Доп. барьеры vk-turn:** iOS-клиент = sideload + $99/год Apple Dev (TestFlight) либо iSH (нетехнарю нереалистично); живой VK-звонок 24/7; гонка вооружений.
- **Вывод:** оба фронта проиграны в моменте → трек **запаркован**. Граница УТП: «работает, когда другие отвалились» держится для **мягкой** фильтрации (Яндекс-ноды + AmneziaWG), не для жёсткого whitelist. Листу ожидания — честно «пока нет».

## ▶ Следующий заход
- **Whitelist:** запаркован. Revisit-триггер: vk-turn-тулза/форк починят VK-капчу (репо активно) → ретест быстрый (сервер за ~30 мин, путь в памяти). Опц.: авто-проверка репо на фикс.
- **Очередь:** #3 рефактор — гейт «после 06-23» **открыт** → scope-оценка монолитов `bot/main.py`/`web/app.py`. Альтернативы: #5 email (нужны ответы владельца), #8-хвост.
- **На владельце:** 🔴 пополнить Яндекс (грант кончился 06-19, суспенд yc/yc2 ~начало июля); честно сказать листу ожидания про БС.
