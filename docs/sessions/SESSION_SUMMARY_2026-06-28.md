# SESSION SUMMARY — 2026-06-28 — Инцидент VLESS-блока РЕШЁН (воскрешение)

> Многодневная сессия 06-26 → 06-28. **Главное: блок 25.06 РЕШЁН — сервис воскрешён, дёшево, на тех же серверах.** Этот документ — полная история «где была проблема и как решили» (запрос владельца). Детальная ранняя форензика — `SESSION_SUMMARY_2026-06-26.md`, НО его вывод «AWG = замена, REALITY мёртв» оказался **НЕВЕРЕН** (см. ниже).

## TL;DR
- 🔴 **25.06 весь VLESS-флот лёг** для РФ-юзеров (~26 ч+ незамеченным — health-check был слеп к реальному трафику).
- ❌ **Ложный диагноз (06-26):** «РКН убил протокол REALITY + IP-блок; спасает только AmneziaWG». Транспорт-поиск (REALITY-альт-порт, Hysteria2 ±obfs) «провалился» → вывод «сервис умирает, держим на AWG».
- 🔑 **Прорыв (06-28):** владелец оплатил конкурента (VPN SPACE) на разведку. Конкурент гоняет ТЕ ЖЕ протоколы (REALITY-443) и они работают у владельца → блок не протокол/IP.
- ✅ **Истинная причина: дефолтные `fp=chrome` + `sni=www.microsoft.com`** (их юзает пол-рынка VPN → ТСПУ зафлагал именно эти fingerprint+SNI). НЕ протокол, НЕ IP, НЕ порт.
- ✅ **Воскрешение:** fp→firefox/qq + чистые разные SNI (ebay/deepl) + vision-TCP. Проверено владельцем (mobile+wifi, продукт-путь) + серверно (eu1 121 коннект).

## Полная история (для агентов)

**1. Инцидент.** 25.06 ~11:00 UTC (14:00 МСК) мгновенный cliff: весь VLESS-REALITY (yc/yc2/eu1/main, TCP/443) перестал нести трафик РФ-юзерам. Вскрыто 06-26 через жалобу платящей (Лара). Health-check был зелёным (проверял порт+сервис, НЕ data-flow) → 26 ч слепоты. Добавлен `check_vless_traffic_flow` (коммит `945daaa`).

**2. Ложный диагноз (важно — как ошиблись).** Серверы отвечали REALITY из вне-РФ; конфиги не менялись. Тесты с РФ-устройства: REALITY-альт-порт 2053 (тот же microsoft-SNI + chrome-fp) — мёртв; Hysteria2 — Wi-Fi-only. Вывод (НЕВЕРНЫЙ): «DPI по протоколу REALITY + IP-блок; AWG = единственная замена». **Ошибка диагностики: меняли порт/транспорт, но НЕ варьировали SNI/fp** — поэтому 2053 на том же заезженном microsoft+chrome тоже умер, и казалось «протокол/IP».

**3. Промежуточные действия (06-26..28).** Компенсация **+7 дней** всем 20 активным (хирургично) + админ-кнопка «🎁 Дать всем дней» (коммит `209038d`). Рассылка «временно AmneziaWG». Прокси-кнопка → текст «недоступно» (MTProxy задавлен; коммит `0f9a0b4`). Telegram-only AWG split-tunnel (отвергнут владельцем — «дублирует обычный VPN»). `vless_traffic_flow` + фикс SKIP при SSH-блипе yc/yc2 (`e8d5f48`).

**4. Прорыв — конкурент-интел.** Владелец оплатил VPN SPACE (@vpn_spacev_bot, панель **Remnawave**) на разведку. Их конфиги (вытащены из подписки/MTProxy-линка): REALITY-vision-TCP/443 с `sni=ebay.com`/`deepl.com` + `fp=firefox`/`qq` (**РАЗНЫЕ** на узел), Hysteria2-443+h3+реальный серт без obfs, MTProxy-443+Fake-TLS под `ads.x5.ru` (белый РФ-домен X5). Ключ: ТЕ ЖЕ протоколы работают у конкурента на сети владельца → блок не протокол/IP.

**5. Изоляция причины (тесты владельца, по одной переменной).** REALITY+ebay на yc:2053 → ожил (Wi-Fi+мобила) на ТОМ ЖЕ yc-IP, где microsoft умер → **не IP**. eu1:2053+ebay+firefox → ожил → **не немецкий-IP**. eu1:**443**+ebay+vision+**chrome** (продакшн-конфиг) → МЁРТВ; ровно то же с **firefox** → ОЖИЛ. → **убийца = `fp=chrome`** (+ заезженный `sni=www.microsoft.com`). Дефолты, которые ТСПУ флагает первыми.

**6. Воскрешение / раскатка.** Узлы: eu1:443 realitySettings serverNames+dest → ebay.com, main:443 → deepl.com. `/sub` (`web/app.py:_build_subscription_links`, коммит `1d2063f`): оставлены **eu1+main**, yc/yc2 убраны; env-шаблоны (на сервере, не в git): eu1 `sni=ebay&fp=firefox`, main `sni=deepl&fp=qq`. Проверено: регенерённая подписка → продукт-путь работает у владельца (mobile+wifi) + 121 реальный коннект на eu1 (`tid_135366416@kronos`).

**7. Закалка (Этап 3).** Разные SNI (ebay/deepl) И fp (firefox/qq) по узлам — один флаг РКН не убьёт оба (моно-chrome нас и убил). **Остаток:** репойнт `vless_traffic_flow` на eu1(локально)+main вместо дропнутых yc/yc2 (сейчас завис в FAIL — следит за мёртвыми узлами, новых алертов не шлёт); чистка бот-флоу «📡 Мобильный» (мёртвые прямые yc-ссылки); список запасных чистых SNI.

**8. Косты (Этап 4).** YC (yc/yc2) = корень убытка (метрир. Яндекс-трафик ~−2300₽/мес). REALITY на YC тоже воскрешаем сменой fp/SNI, НО eu1 (Fornex flat-rate) тянет → **YC под снос:** перенос `/sub`-фронта с yc/yc2 (правило #0.1), снятие зависимостей health-check, удаление VM.

## Ключевые уроки (чтоб не повторить)
1. **НЕ использовать дефолты** `fp=chrome` + `sni=www.microsoft.com` — их юзает пол-рынка VPN, ТСПУ флагает первыми. Брать чистые SNI (ebay/deepl/…) + не-chrome fp (firefox/qq/safari).
2. **Варьировать fp+SNI ПО УЗЛАМ** — моно-конфиг = один флаг убивает весь флот.
3. **Изолировать переменные при диагностике** — меняли SNI+порт+транспорт+fp разом → неверный вывод «протокол мёртв». Менять по одной.
4. **Конкурент-интел разблокировало** — платная разведка рабочего сервиса окупилась мгновенно. «Всегда есть способ» (владелец) подтвердилось.
5. **health-check мерит реальный data-flow**, не только порт+сервис (иначе 26 ч слепоты).

## Память
- 🔑 `project_vpn_reality_revival_sni` — главное (причина + фикс).
- `project_vpn_rkn_reality_dpi_block_0625` — исправлен (вывод «по протоколу» помечен неверным).
- `project_vpn_transport_after_block` — устарел (AWG ≠ единственная замена).

## Состояние на конец сессии
- ✅ Сервис воскрешён: **eu1=ebay/firefox, main=deepl/qq**, `/sub` = 2 узла, yc/yc2 убраны (под снос).
- ◀ Остаток Этапа 3: репойнт `vless_traffic_flow`, чистка бот-флоу, запасные SNI.
- ◀ Этап 4: снос YC (−2300₽).
- ⏳ Параллельно: интел Daniil (жёсткий БС/whitelist — отдельная стена, SNI её НЕ решает).
