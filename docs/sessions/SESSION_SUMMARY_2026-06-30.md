# SESSION SUMMARY — 2026-06-30 — vk-turn Phase B валидирован + жёсткий-БС тест упёрся в топологию

> Продолжение 06-29 (триаж уведомления + Phase A: капча снята форком NikKuz99 v1.8.4). Сегодня:
> подняли сервер, доказали ПОЛНЫЙ тоннель end-to-end, но реальный БС-тест (Аня) сорвался по
> топологии устройства. Итог: тех-часть закрыта полностью, пробой жёсткого БС — отложен до реального БС.

## TL;DR
- ✅ **Phase B — тоннель доказан end-to-end** на ОТКРЫТОЙ сети (прогон владельца, VPN off): client→VK-релей→eu1→WireGuard→интернет, сайт грузится. Серверно: `wg-vkt` пир handshake + **1.72/1.86 MiB** реального трафика; server.log — VK-релей (193.203.43.47) доставил потоки, DTLS done. **Тех-часть закрыта** (капча + тоннель).
- ❌ **Жёсткий-БС тест (Аня) НЕ состоялся — топологический тупик.** Её whitelist только на МОБИЛЕ (айфон); ноут в нормальной сети; тетеринг невозможен (нет сигнала где ноут) → единственное БС-устройство = iPhone → vk-turn на iOS = iSH/sideload (нереалистично нетехнарю; рабочий-с-капчей клиент только CLI).
- 🧭 **Вывод:** пробой жёсткого БС тестируется ТОЛЬКО на реальном БС-устройстве (симуляция = ложная уверенность, не воспроизводит DPI релея). **План:** владелец сам протестит, когда БС введут на его сети (под БС Wi-Fi+VPN к Claude сохраняется → координация работает).
- 🛠 **Готовность:** dormant-kit на eu1 (`/opt/vkturn-test/up.sh|down.sh`, подъём ~1 мин, ключи фиксированы → конфиг валиден), рунбук `C:\Users\krono\vkturn\RUNBOOK-when-BS-hits.txt`, `VK-TURN-VALIDATE.bat` + `nikita-vkt.conf` (импортирован в WireGuard) на машине.

## Что делали (для агентов)
1. **Сервер eu1 (изолированно):** `wg-quick wg-vkt` (10.99.99.0/24, ListenPort 51899, NAT MASQUERADE→eth0 в PostUp/PostDown) + `server-linux-amd64 -listen 0.0.0.0:56000 -connect 127.0.0.1:51899`. Пиры: владелец 10.99.99.2, Аня 10.99.99.3. Прод (бот/web/AWG/Xray) не тронут, health зелёный.
2. **Клиентский WG-конфиг:** Endpoint=127.0.0.1:9000 (локальный vk-turn-клиент), AllowedIPs = 772-CIDR VK-исключение из README (VK-релей-диапазоны идут DIRECT, иначе цикл; проверено: 90.156.232.0/21 = gap), MTU 1280, DNS 1.1.1.1.
3. **Прогон владельца:** ложная тревога №1 — переиспользовал утренний VK-звонок → `error_code:9000 Call not found` (звонок умер пустым). Создал свежий, остался в нём → прошло. Сайт через тоннель грузится. Серверно подтверждено.
4. **Аня:** не смогла — нет тетеринга, БС только на айфоне (см. TL;DR).
5. **Свёртка:** `down.sh` остановил сервер + `wg-quick down wg-vkt` (NAT снят); бинарь+скрипты+конфиг dormant на диске для подъёма за ~1 мин.

## Ключевые уроки
- VK-звонок живёт, пока в нём кто-то есть; пустой VK закрывает → «Call not found». Для теста — создать свежий и остаться в нём.
- WG-блок РФ vk-turn НЕ мешает (WG едет по loopback внутри «VK-звонка»); подтверждено — падало на VK-авторизации, до WG-трафика.
- Жёсткий БС нельзя сымитировать — нужен реальный.
- iOS — худшая платформа для инструментовки (iSH); Android проще. У тест-контакта айфон → тупик.

## Память
- `project_vpn_hard_whitelist_unbeaten` — обновлена (Phase B валидирован, топологический тупик, план self-test, dormant-kit).
