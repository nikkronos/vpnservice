# Telegram bot settings
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id

# Ссылка MTProto-прокси для Telegram (команда /proxy в боте). Опционально.
# MTPROTO_PROXY_LINK=tg://proxy?server=185.21.8.91&port=443&secret=...

# Server / VPN node (для автоматизации)
# ВАЖНО: реальные значения прописываются только в env_vars.txt на сервере/локально, этот файл — пример.
VPN_SERVER_HOST=81.200.146.32
VPN_SERVER_USER=root

# WireGuard settings для ноды "main" (Россия, Timeweb)
# Публичный ключ сервера (можно получить командой `wg show wg0 public-key`)
WG_SERVER_PUBLIC_KEY=your_server_public_key_here

# Интерфейс и сеть WireGuard для main
WG_INTERFACE=wg0
WG_NETWORK_CIDR=10.0.0.0/24

# Параметры подключения клиентов к main
WG_ENDPOINT_HOST=81.200.146.32
WG_ENDPOINT_PORT=51820
WG_DNS=1.1.1.1

# WireGuard settings для второй ноды "eu1" (Европа)
# Раскомментируй и заполни, когда вторая нода будет готова:
# WG_EU1_SERVER_PUBLIC_KEY=your_eu_server_public_key_here
# WG_EU1_INTERFACE=wg0
# WG_EU1_NETWORK_CIDR=10.0.0.0/24
# WG_EU1_ENDPOINT_HOST=your_eu_server_ip
# WG_EU1_ENDPOINT_PORT=51820
# WG_EU1_DNS=1.1.1.1
# WG_EU1_MTU=1280
# # Опционально: MTU в клиентском конфиге (для eu1 иногда помогает при проблемах с маршрутизацией). Не задавать — использовать по умолчанию.
#
# Если бот работает на другом сервере, а EU-нода на удалённом VPS:
# WG_EU1_SSH_HOST=your_eu_server_ip
# WG_EU1_SSH_USER=root
# WG_EU1_SSH_KEY_PATH=/path/to/ssh/key  # опционально, если не используется стандартный ~/.ssh/id_rsa
#
# Скрипт добавления редиректа для VPN+GPT на eu1 (бот вызывает по SSH). По умолчанию /opt/vpnservice/scripts/add-ss-redirect.sh
# WG_EU1_ADD_SS_REDIRECT_SCRIPT=/opt/vpnservice/scripts/add-ss-redirect.sh
#
# Примечание: Unified профиль (profile_type="unified") не требует дополнительных переменных окружения.
# Настройка на сервере eu1: ipset unified_ss_dst + правило iptables (см. docs/scripts/setup-unified-iptables.sh.example).
# Скрипт обновления IP: docs/scripts/update-unified-ss-ips.sh.example (запускать по cron).
#
# AmneziaWG для eu1 (автоматическая выдача и регенерация конфигов ботом):
# Если задан AMNEZIAWG_EU1_ADD_CLIENT_SCRIPT, бот для Европы вызывает скрипт по SSH и выдаёт AmneziaWG .conf.
# Скрипт add: один аргумент client_ip; вывод: первая строка PUBKEY=<pubkey>, далее — клиентский .conf.
# Для регенерации (/regen): бот удаляет старый peer (awg set interface peer remove или скрипт remove) и вызывает add с тем же IP.
# Примеры скриптов: docs/scripts/amneziawg-add-client.sh.example, amneziawg-remove-client.sh.example.
# Пошаговая настройка: docs/amneziawg-bot-automation-setup.md
# AMNEZIAWG_EU1_ADD_CLIENT_SCRIPT=/opt/vpnservice/scripts/amneziawg-add-client.sh
# AMNEZIAWG_EU1_NETWORK_CIDR=10.1.0.0/24
# AMNEZIAWG_EU1_INTERFACE=awg0
# AMNEZIAWG_EU1_REMOVE_CLIENT_SCRIPT=/opt/vpnservice/scripts/amneziawg-remove-client.sh

